version: 2

build:
  image: latest

# Build PDF
formats:
  - pdf

python:
  version: 3.8
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: python
  system_packages: true